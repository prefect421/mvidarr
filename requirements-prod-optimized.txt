# MVidarr Production Dependencies - OPTIMIZED
# Removed unused heavy dependencies for Docker size optimization

# Core Framework
Flask==2.3.3
Flask-CORS>=6.0.0
Flask-Session==0.5.0
Flask-Caching==2.1.0
Flask-Limiter==3.5.0

# Database
SQLAlchemy==2.0.23
PyMySQL==1.1.1
alembic==1.13.1
# Removed: mysqlclient (PyMySQL provides same functionality)

# HTTP and API  
requests==2.32.4
urllib3==2.5.0
aiohttp==3.10.11
# Removed: httpx (redundant with requests)

# Image Processing
Pillow==10.3.0
# Removed: opencv-python-headless (120MB+ unused dependency)

# Media Processing
python-ffmpeg==2.0.12
# Removed: moviepy (unused heavy dependency)

# Data Processing
python-dateutil==2.8.2
python-dotenv==1.0.0
# Removed: pydantic (unused, marshmallow provides validation)
marshmallow==3.20.1

# Background Tasks - REMOVED UNUSED STACK
# Removed: celery==5.3.4 (unused background task system)
# Removed: redis==5.0.1 (unused with celery)
# Removed: flower==2.0.1 (unused celery monitoring)

# Security
bcrypt==4.1.2
cryptography>=42.0.0
PyJWT==2.8.0
pyotp==2.9.0
qrcode==7.4.2
defusedxml==0.7.1

# Utilities
click==8.1.7
colorama==0.4.6
tqdm==4.66.3
psutil==5.9.6
schedule==1.2.0

# Monitoring and Logging
prometheus-client==0.19.0
structlog==23.2.0
sentry-sdk==2.8.0

# Production Server
gunicorn==23.0.0
gevent==23.9.1

# Additional utilities
yarl==1.9.4
aiofiles==23.2.1
python-slugify==8.0.1
bleach==6.1.0
humanize==4.9.0

# Video processing (legacy support)
yt-dlp>=2024.12.0