# FastAPI Core Framework
fastapi==0.141.1
starlette>=1.6.0  # Security: PYSEC-2026-161 Host header injection fix, GHSA-7f5h-v6xp-fcq8, GHSA-f96h-pmfr-66vw
typing-inspection>=0.4.2
uvicorn[standard]==0.52.3

# ASGI Server and WebSocket support
uvicorn==0.52.3
websockets==17.0.1

# Template engine (compatible with FastAPI)
jinja2==3.1.6

# Static files serving
aiofiles==25.1.0

# Validation and serialization  
pydantic==2.13.4
pydantic-settings==2.14.2  # Security: GHSA-4xgf-cpjx-pc3j fix

# HTTP client for async requests
httpx==0.28.1

# Database - Async Support
sqlalchemy[asyncio]>=2.0.51
aiomysql>=0.3.2
cryptography>=50.0.0
python-multipart>=0.0.32  # Security: CVE-2026-53539 quadratic CPU DoS fix, CVE-2026-53538 parameter smuggling fix, CVE-2026-53537 Content-Disposition smuggling fix, CVE-2026-45152 negative Content-Length fix

# Keep pymysql for Flask compatibility during migration
pymysql>=1.2.0

# Security utilities
werkzeug>=3.1.8  # Security: CVE-2026-21860

# Background job dependencies (already installed)
# asyncio (built-in)
# concurrent.futures (built-in)