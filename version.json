{
  "version": "0.12.1",
  "build_date": "2026-02-12T15:43:17.720579",
  "git_commit": "d0e4db2",
  "git_branch": "dev",
  "release_name": "v0.12.1 - Security Hardening Stabilization",
  "features": [
    "Fix: Login page updated to use correct auth endpoint (was POSTing to removed /test-login)",
    "Fix: WAF false positives disabled - SQL injection regex blocked URLs, cookies, Range headers",
    "Fix: Video streaming restored (Range header no longer blocked by security middleware)",
    "Fix: YouTube playlist sync now detects new videos (broken incremental sync replaced with full fetch)",
    "Fix: Playlist add error messages now show actual server detail instead of generic HTTP 400",
    "Fix: Flask-to-FastAPI session bridge for consistent auth across both frameworks",
    "Fix: Rate limiting tuned for normal page loads (300/min, static files exempt)",
    "Fix: Migration 023 guarded against duplicate column error",
    "CI/CD: Code formatted with black 24.3.0 (28 files reformatted to match CI)"
  ],
  "resolved_issues": [
    "Login page POST to removed /test-login endpoint",
    "SecurityValidationMiddleware blocking legitimate requests (URLs, cookies, headers)",
    "Video stream 400 errors from Range header WAF false positive",
    "YouTube playlist sync returning 0 new videos (broken incremental logic)",
    "429 Too Many Requests from dual stacked rate limiters",
    "401 on FastAPI endpoints when logged in via Flask session",
    "CI/CD black formatting check failure (28 files)"
  ]
}
