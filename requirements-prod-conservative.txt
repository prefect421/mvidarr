# MVidarr Production Dependencies - CONSERVATIVE OPTIMIZATION
# Only removing dependencies that are definitively unused

# Core Framework
Flask==2.3.3
Flask-CORS>=6.0.0
Flask-Session==0.5.0
Flask-Caching==2.1.0
Flask-Limiter==3.5.0

# Database
SQLAlchemy==2.0.23
PyMySQL==1.1.1
alembic==1.13.1
mysqlclient>=2.2.0

# HTTP and API
requests==2.32.4
urllib3==2.5.0
aiohttp==3.10.11
# CONSERVATIVE: Keep httpx for now (small, may be used in services)
httpx==0.25.2

# Image Processing
Pillow==10.3.0
# REMOVED: opencv-python-headless (definitely unused, saves ~120MB)

# Media Processing
python-ffmpeg==2.0.12
# REMOVED: moviepy (definitely unused, saves ~80MB)

# Data Processing
python-dateutil==2.8.2
python-dotenv==1.0.0
pydantic==2.5.0
marshmallow==3.20.1

# Background Tasks - ANALYSIS NEEDED
# REMOVED: celery, redis, flower (no celery tasks found, saves ~30MB)

# Security
bcrypt==4.1.2
cryptography>=42.0.0
PyJWT==2.8.0
pyotp==2.9.0
qrcode==7.4.2
defusedxml==0.7.1

# Utilities
click==8.1.7
colorama==0.4.6
tqdm==4.66.3
psutil==5.9.6
schedule==1.2.0

# Monitoring and Logging
prometheus-client==0.19.0
structlog==23.2.0
sentry-sdk==2.8.0

# Production Server
gunicorn==23.0.0
gevent==23.9.1

# Additional utilities
yarl==1.9.4
aiofiles==23.2.1
python-slugify==8.0.1
bleach==6.1.0
humanize==4.9.0

# Video processing (legacy support)
yt-dlp>=2024.1.0